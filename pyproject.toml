[project]
name = "agent-factory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "szymon", email = "madej.szymon@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-adk>=2.9.0",
]

[project.scripts]
agent-factory = "agent_factory:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
